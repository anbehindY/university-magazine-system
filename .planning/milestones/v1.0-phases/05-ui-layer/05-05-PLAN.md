---
phase: 05-ui-layer
plan: "05"
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04"]
files_modified: []
autonomous: false
requirements: [GUEST-01, GUEST-02]

must_haves:
  truths:
    - "All role-specific pages are visually correct and functional"
    - "Sidebar navigation matches the logged-in user's role"
    - "Coordinator slide-over panel works with toggle, notes, and comments"
    - "Manager ZIP download works after final closure"
    - "Guest sees read-only selected submissions for their faculty"
    - "Reports page shows correct data for the user's role scope"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of all Phase 5 UI pages across all roles.

Purpose: Confirm that all role-scoped pages render correctly, interactions work, and no visual or functional issues remain before marking Phase 5 complete.
Output: User confirmation that all pages pass visual inspection.
</objective>

<execution_context>
@/home/alfie/.claude/get-shit-done/workflows/execute-plan.md
@/home/alfie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-layer/05-01-SUMMARY.md
@.planning/phases/05-ui-layer/05-02-SUMMARY.md
@.planning/phases/05-ui-layer/05-03-SUMMARY.md
@.planning/phases/05-ui-layer/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the project and verify all pages compile</name>
  <files></files>
  <action>
Run `pnpm build` to verify the entire application builds successfully with all new pages included.

If build errors occur:
1. Read the error output carefully
2. Fix any TypeScript or import errors in the files created by Plans 01-04
3. Re-run `pnpm build` until clean

This task ensures all pages are valid Next.js routes and compile correctly before user verification.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>pnpm build completes successfully with zero errors; all new pages are compiled as valid routes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all role-specific pages</name>
  <files></files>
  <action>
Present the verification checklist to the user. All Phase 5 UI pages have been built:
- Role-based sidebar navigation (coordinator, manager, guest, student, admin)
- Coordinator submissions page with slide-over panel (isSelected toggle, notes, comment thread with SWR polling)
- Manager submissions page with faculty filter and ZIP download button
- Guest submissions page (read-only selected articles)
- Reports page with Statistics and Exceptions tabs, academic year selector

User should verify by starting `pnpm dev` and testing each role:

1. **Coordinator view** — Log in as a Marketing Coordinator:
   - Sidebar shows "Submissions" and "Reports" links
   - /coordinator/submissions shows submitted work table
   - Click row opens slide-over panel
   - Toggle "Selected for Publication" persists on refresh
   - Notes textarea saves
   - Post a comment — appears immediately
   - Wait 15s — SWR poll runs without errors

2. **Manager view** — Log in as a Marketing Manager:
   - Sidebar shows "Selected Submissions" and "Reports" links
   - /manager/submissions has faculty dropdown filter
   - Download ZIP works after final closure (disabled before with tooltip)

3. **Guest view** — Log in as a Guest:
   - Sidebar shows "Selected Articles" and "Reports" links
   - /guest/submissions shows read-only table (no edit controls)

4. **Reports** — /reports (any authorized role):
   - Statistics tab: summary cards + data table
   - Exceptions tab: color-coded rows (amber/red) with overdue toggle

5. **Admin view** — Sidebar shows "Closure Dates", "Upload Rules", "User Management" only
  </action>
  <verify>
    <manual>User confirms all pages pass visual and functional inspection</manual>
  </verify>
  <done>User has approved all Phase 5 UI pages as visually correct and functionally working across all roles.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- User confirms all pages render correctly across roles
</verification>

<success_criteria>
The application builds cleanly and the user confirms all Phase 5 pages are visually correct and functionally working across all roles.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-layer/05-05-SUMMARY.md`
</output>
