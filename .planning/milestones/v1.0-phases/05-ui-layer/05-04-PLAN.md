---
phase: 05-ui-layer
plan: "04"
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - app/(management)/reports/page.tsx
autonomous: true
requirements: [GUEST-02]

must_haves:
  truths:
    - "Reports page has two tabs: Statistics and Exceptions"
    - "Statistics tab shows summary cards (total submissions, total contributors) and a sortable data table with per-faculty breakdown"
    - "Exception tab shows color-coded rows — amber for no comment, red for 14+ days overdue"
    - "Exception tab has a toggle or sub-tabs to switch between All exceptions and Overdue only"
    - "Academic year selector dropdown defaults to active year and re-fetches data when changed"
    - "Reports page is accessible to coordinator, manager, admin, and guest roles"
  artifacts:
    - path: "app/(management)/reports/page.tsx"
      provides: "Reports page with Statistics and Exceptions tabs"
      min_lines: 150
  key_links:
    - from: "app/(management)/reports/page.tsx"
      to: "/api/reports?type=submissions"
      via: "fetch with academicYearId param for statistics data"
      pattern: "fetch.*api/reports.*type=submissions"
    - from: "app/(management)/reports/page.tsx"
      to: "/api/reports?type=exceptions"
      via: "fetch with academicYearId and optional overdue param for exception data"
      pattern: "fetch.*api/reports.*type=exceptions"
    - from: "app/(management)/reports/page.tsx"
      to: "/api/academic-years"
      via: "fetch for year selector dropdown with id field"
      pattern: "fetch.*api/academic-years"
---

<objective>
Build the reports page with Statistics and Exceptions tabs, academic year selector, summary cards, sortable data table, and color-coded exception rows.

Purpose: All authorized roles (coordinator, manager, admin, guest) need to view statistical and exception reports scoped to their access level — this fulfills GUEST-02 (guest can view reports scoped to their faculty).
Output: Fully functional reports page at /reports with tabs, year selector, and role-scoped data display.
</objective>

<execution_context>
@/home/alfie/.claude/get-shit-done/workflows/execute-plan.md
@/home/alfie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-layer/05-RESEARCH.md
@.planning/phases/05-ui-layer/05-01-SUMMARY.md

@app/api/reports/route.ts
@app/api/academic-years/route.ts
@components/ui/tabs.tsx
@components/ui/card.tsx
@components/ui/select.tsx
@components/ui/badge.tsx
@app/(management)/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reports page with Statistics tab</name>
  <files>app/(management)/reports/page.tsx</files>
  <action>
Create `app/(management)/reports/page.tsx` as a `"use client"` page.

**Academic year selector (per locked decision):**
- Fetch `/api/academic-years` on mount to get the active year (now returns `id` per Plan 01 fix)
- Set `activeYearId` state from the response
- Use `Select` component (from `components/ui/select.tsx`) at the top of the page
- For now, the selector shows only the active year (since `/api/academic-years` returns only the active year). The selector is still useful to display which year is shown and can be extended later for multi-year support.
- Changing the selection triggers a re-fetch of both statistics and exceptions data with `?academicYearId=X`

**Tabs setup (per locked decision — single /reports page with tabs):**
- Import `Tabs, TabsList, TabsTrigger, TabsContent` from `@/components/ui/tabs` (created in Plan 01)
- Default active tab: "Statistics"
- Two triggers: "Statistics" and "Exceptions"

**Statistics tab content:**
- Fetch `/api/reports?type=submissions&academicYearId=${yearId}` when tab is active or year changes
- Response shape: `{ data: Array<{ facultyId, facultyName, submissionCount, percentageOfTotal, distinctContributors }> }`

- **Summary cards at top (per locked decision):**
  - Use `Card` component (from `components/ui/card.tsx`)
  - Card 1: "Total Submissions" — sum of all `submissionCount` values
  - Card 2: "Total Contributors" — sum of all `distinctContributors` values
  - Card 3 (optional): "Faculties" — count of rows

- **Data table below (per locked decision — sortable):**
  - Columns: Faculty Name, Submissions, Percentage of Total (%), Contributors
  - Default sort: by Faculty Name ascending
  - Implement client-side sorting by clicking column headers (toggle asc/desc)
  - Use `<th onClick>` with sort state to track column and direction
  - Display percentage with one decimal (e.g., "45.2%")
  - Use existing Tailwind table patterns

**Loading state:** Skeleton placeholders for cards and table rows
**Error state:** Inline error message
**Empty state:** "No submission data available for this academic year"
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Reports page renders with Tabs component; Statistics tab shows summary cards and a sortable data table fetched from /api/reports?type=submissions; academic year selector is present; TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Exceptions tab with color-coded rows and overdue toggle</name>
  <files>app/(management)/reports/page.tsx</files>
  <action>
Add the Exceptions tab content to the existing reports page.

**Exceptions data loading:**
- Fetch `/api/reports?type=exceptions&academicYearId=${yearId}` when the Exceptions tab is active or year changes
- For overdue filter: fetch `/api/reports?type=exceptions&academicYearId=${yearId}&overdue=true`
- Response shape: `{ data: Array<{ id, title, studentName, facultyName, submittedAt, daysSinceSubmission }> }`
- Store both "all" and "overdue" data in separate state, or use a single state with the `overdue` param

**Sub-toggle (per locked decision — "Tabs or toggle to switch between 'All exceptions' and 'Overdue only'"):**
- Use a simple pair of buttons or a small `TabsList` within the Exceptions tab
- Two options: "All Exceptions" and "Overdue Only (14+ days)"
- Toggling refetches with/without `&overdue=true` param (or filters client-side if both datasets are small)

**Exception table:**
- Columns: Student Name, Title, Faculty, Submitted Date, Days Since Submission
- **Color-coded rows (per locked decision):**
  - `daysSinceSubmission >= 14` → red background: `bg-red-50` with `text-red-900` (overdue)
  - `daysSinceSubmission < 14` → amber background: `bg-amber-50` with `text-amber-900` (no comment)
  - Use existing project color palette (amber/red already in use)
- Sort by `daysSinceSubmission` descending by default (most overdue first)

**Empty state:** "No exceptions found — all submissions have coordinator comments"

**Error state:** Inline error message consistent with Statistics tab

**Important:** The reports API handles role scoping server-side (coordinator/guest see their faculty only, manager/admin see all). The UI does NOT need to add any role filtering — just display what the API returns.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify that exception rows use bg-red-50 for 14+ days and bg-amber-50 for under 14 days</manual>
  </verify>
  <done>Exceptions tab shows color-coded rows (amber for no-comment, red for 14+ days overdue); toggle switches between All exceptions and Overdue only; data fetched from /api/reports?type=exceptions with optional overdue param; TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `app/(management)/reports/page.tsx` exists and is >150 lines
- Page imports `Tabs, TabsList, TabsTrigger, TabsContent` from `@/components/ui/tabs`
- Page fetches from `/api/reports?type=submissions` and `/api/reports?type=exceptions`
- Page fetches from `/api/academic-years` for the year selector
- Statistics tab has summary cards using `Card` component
- Statistics tab has a sortable table with column header click handlers
- Exceptions tab has color-coded rows using `bg-red-50` and `bg-amber-50`
- Exceptions tab has a toggle for All vs Overdue
</verification>

<success_criteria>
Any authorized role (coordinator, manager, admin, guest) visiting /reports sees a tabbed page with Statistics showing summary cards and a sortable faculty breakdown table, and Exceptions showing color-coded rows with an overdue toggle. The academic year selector defaults to the active year. Data is scoped by the API based on the user's role.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-layer/05-04-SUMMARY.md`
</output>
