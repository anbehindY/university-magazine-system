---
phase: 06-critical-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/submissions/route.ts
  - app/(student)/submissions/page.tsx
autonomous: true
requirements:
  - COORD-02

must_haves:
  truths:
    - "A student can enter a title when creating or editing a submission and the title is persisted to the database"
    - "When a coordinator receives an email notification for a new submission the subject line includes the student-provided title"
    - "Editing an existing submission restores the previously saved title in the input field"
    - "Draft title is preserved in localStorage and restored on page reload"
  artifacts:
    - path: "app/api/submissions/route.ts"
      provides: "SubmissionPayload type with title field, title wired to prisma create and update"
      contains: "title"
    - path: "app/(student)/submissions/page.tsx"
      provides: "Title input field in submission form with state, localStorage persistence, edit restoration"
      contains: "setTitle"
  key_links:
    - from: "app/(student)/submissions/page.tsx"
      to: "app/api/submissions/route.ts"
      via: "title field sent in POST/PUT fetch body"
      pattern: "title"
    - from: "app/api/submissions/route.ts"
      to: "prisma.submission.create"
      via: "body.title passed to prisma create data"
      pattern: "title.*body\\.title"
---

<objective>
Wire the submission title field end-to-end: add title to the API payload type and Prisma operations, then add a title input to the student submission form with state management, localStorage draft persistence, and edit restoration.

Purpose: COORD-02 requires that coordinator email notifications include the student-provided title. The email template already uses `submission.title`, but the student form never sends a title. This plan closes the gap so titles flow from student input to database to email.
Output: Two surgical edits — API type + Prisma wiring, and student form UI with title input.
</objective>

<execution_context>
@/home/alfie/.claude/get-shit-done/workflows/execute-plan.md
@/home/alfie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-critical-fixes/06-RESEARCH.md

@app/api/submissions/route.ts
@app/(student)/submissions/page.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add title to SubmissionPayload type and wire to Prisma create/update</name>
  <files>
    app/api/submissions/route.ts
  </files>
  <action>
**Submissions API route (`app/api/submissions/route.ts`):**

1. **Add `title` to the `SubmissionPayload` type:**
   ```typescript
   type SubmissionPayload = {
     id?: string;
     agreed?: boolean;
     title?: string | null;  // Add this field
     notes?: string | null;
     status?: "DRAFT" | "SUBMITTED";
   };
   ```

2. **Wire `title` into the POST handler's `prisma.submission.create()` call.** Add `title: body.title ?? null` to the `data` object, alongside the existing `notes: body.notes ?? null` line.

3. **Wire `title` into the PUT handler's update logic.** Find the section where `updateData` is built (where `body.notes` is conditionally added). Add a similar conditional for title:
   ```typescript
   if (body.title !== undefined) {
     updateData.title = body.title ?? null;
   }
   ```
   Place this alongside the existing `if (body.notes !== undefined)` block.

The `Submission.title` column already exists in the Prisma schema as `String? @db.Text` — no migration needed. The email notification in the PUT handler already reads `updatedSubmission.title` for the email subject line, so once title is persisted via create/update, emails will automatically include the student-provided title.

Do NOT modify the email logic, closure gates, auth checks, or any other existing behavior. This is a type extension + data wiring only.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify the SubmissionPayload type includes title. Verify prisma.submission.create data includes body.title. Verify the PUT updateData conditionally includes title.</manual>
  </verify>
  <done>
    - SubmissionPayload type includes `title?: string | null`
    - POST handler passes `title: body.title ?? null` to prisma.submission.create
    - PUT handler conditionally adds title to updateData when body.title is present
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add title input to student submission form with state, localStorage, and edit restoration</name>
  <files>
    app/(student)/submissions/page.tsx
  </files>
  <action>
**Student submissions page (`app/(student)/submissions/page.tsx`):**

Follow the existing `notes` pattern exactly for `title`. The form already has state variables, localStorage persistence, edit restoration, and form reset — title mirrors each of these.

1. **Add state variable:**
   ```typescript
   const [title, setTitle] = useState("");
   ```
   Place alongside the existing `notes` state declaration.

2. **Add `title` to the submissions list type.** Find the `useState` that types the submissions array and add `title: string | null` to the type shape (alongside the existing `notes: string | null`).

3. **Restore title from localStorage.** In the useEffect that loads from `localStorage` (using `DRAFT_STORAGE_KEY`), add:
   ```typescript
   setTitle(parsed.title ?? "");
   ```
   alongside the existing `setNotes(parsed.notes ?? "")` or similar localStorage restoration line.

4. **Restore title in `startEditSubmission()`.** Find the function that populates form fields when editing an existing submission (it calls `setNotes(submission.notes ?? "")`). Add:
   ```typescript
   setTitle(submission.title ?? "");
   ```
   right next to the `setNotes` call.

5. **Reset title in `resetForm()` (or equivalent form reset function).** Add:
   ```typescript
   setTitle("");
   ```
   alongside the existing `setNotes("")` reset.

6. **Include title in the POST/PUT fetch body.** In `saveDraftToDb()` (or the equivalent function that calls fetch to `/api/submissions`), add `title` to the `JSON.stringify()` body object:
   ```typescript
   body: JSON.stringify({
     // existing fields...
     title,
     // ...
   }),
   ```

7. **Include title in localStorage save.** Find where the draft is saved to `localStorage` (the `JSON.stringify` call with `agreed`, `notes`, `fileNames`, etc.). Add `title` to the saved object:
   ```typescript
   JSON.stringify({ agreed, title, notes, fileNames, savedAt, submissionId })
   ```

8. **Render the title input field in the form UI.** Add a labeled `Input` component for title above the notes field (or as the first form field after the terms agreement). Use the existing shadcn/ui `Input` component:
   ```tsx
   <div className="space-y-1">
     <Label htmlFor="title">Title</Label>
     <Input
       id="title"
       placeholder="Give your submission a title (optional)"
       value={title}
       onChange={(e) => setTitle(e.target.value)}
     />
   </div>
   ```
   Import `Label` from `@/components/ui/label` if not already imported (check existing imports first). Import `Input` from `@/components/ui/input` if not already imported.

9. **Display title in the submissions list.** Where each submission is rendered in the list (likely showing status and notes), also display `submission.title` if present — this gives the student visual confirmation that their title was saved.

Title is optional (matches the `String?` schema). No validation beyond trimming is needed. An empty title results in `null` being stored, which preserves the existing "Untitled" fallback in the email template.

Do NOT modify the file upload logic, closure enforcement, terms agreement, or any other existing form behavior.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Start the dev server (`npm run dev`), navigate to /submissions as a Student. Verify: (1) Title input appears in the form. (2) Entering a title and saving persists it. (3) Refreshing the page restores the title from localStorage. (4) Editing an existing submission loads the saved title. (5) Resetting the form clears the title.</manual>
  </verify>
  <done>
    - Title input renders in the student submission form
    - Title is sent in POST and PUT requests to /api/submissions
    - Title persists in localStorage drafts and restores on page reload
    - Editing an existing submission restores its saved title
    - Form reset clears the title field
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors in any modified file
2. `npm run build` succeeds — all routes compile
3. Manual: Create a new submission with a title, verify it appears in the database
4. Manual: Submit the submission, verify the coordinator email subject includes the title
5. Manual: Edit an existing submission, verify the title field is pre-populated
6. Manual: Save a draft, refresh the page, verify title restores from localStorage
</verification>

<success_criteria>
- SubmissionPayload type includes title field
- Prisma create and update operations include title
- Student form has a working title input with state management
- Title persists through localStorage drafts and edit restoration
- Coordinator email notifications include student-provided titles (existing wiring, now fed by real data)
</success_criteria>

<output>
After completion, create `.planning/phases/06-critical-fixes/06-02-SUMMARY.md`
</output>
