---
phase: 06-critical-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/manager/submissions/download/route.ts
  - app/(management)/manager/submissions/page.tsx
autonomous: true
gap_closure: true
requirements: [MGR-02]

must_haves:
  truths:
    - "Marketing Manager can download ZIP at any time without date restriction"
    - "Download ZIP button is always enabled (no disabled state, no Tooltip gate)"
    - "finalClosureDate field remains in the API response (harmless, no removal needed)"
  artifacts:
    - path: "app/api/manager/submissions/download/route.ts"
      provides: "ZIP download route without closure gate"
      contains: "No isPastFinalClosure check"
    - path: "app/(management)/manager/submissions/page.tsx"
      provides: "Always-enabled Download ZIP button"
      contains: "No isPastFinalClosure disabled condition"
  key_links:
    - from: "app/(management)/manager/submissions/page.tsx"
      to: "/api/manager/submissions/download"
      via: "fetch in handleDownloadZip"
      pattern: "fetch.*api/manager/submissions/download"
---

<objective>
Remove the closure date gate from the ZIP download feature so Marketing Managers can download selected submissions at any time.

Purpose: UAT revealed the user wants ZIP download available at any time, not gated by finalClosureDate. The closure gate added in 06-01 must be reversed at both API and UI layers.
Output: Download route with no closure guard; manager UI button always enabled with no Tooltip gate.
</objective>

<execution_context>
@/home/alfie/.claude/get-shit-done/workflows/execute-plan.md
@/home/alfie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-critical-fixes/06-01-SUMMARY.md
@app/api/manager/submissions/download/route.ts
@app/(management)/manager/submissions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove closure gate from ZIP download API route</name>
  <files>app/api/manager/submissions/download/route.ts</files>
  <action>
Remove Guard 3 (the inverted closure gate block) from the GET handler. Specifically:

1. Delete the import of `isPastFinalClosure` from `@/lib/closure-guard` (line 2). If other imports from closure-guard remain, only remove `isPastFinalClosure` from the import list.
2. Delete the entire Guard 3 block (lines 28-34 approximately):
   ```
   if (!(await isPastFinalClosure())) {
     return new Response(
       JSON.stringify({ error: "ZIP download is only available after the final closure date." }),
       { status: 403, headers: { "Content-Type": "application/json" } }
     );
   }
   ```
3. Keep Guard 1 (auth) and Guard 2 (role gate) untouched.
4. Keep all other code (archiver logic, blob fetching, etc.) unchanged.

The result: after auth + role check, the handler proceeds directly to building the ZIP.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit --pretty false 2>&1 | head -20</automated>
    <manual>Confirm the download route no longer references isPastFinalClosure and has no closure gate</manual>
  </verify>
  <done>ZIP download route has only auth + role guards. No closure date check exists. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove disabled state and Tooltip gate from manager UI Download ZIP button</name>
  <files>app/(management)/manager/submissions/page.tsx</files>
  <action>
Remove the closure-gated disabled state and Tooltip from the Download ZIP button. Specifically:

1. Remove the `finalClosureDate` state variable and its setter (`useState<string | null>(null)` and `setFinalClosureDate`).
2. Remove the `isPastFinalClosure` derived boolean (the line: `const isPastFinalClosure = finalClosureDate ? Date.now() > new Date(finalClosureDate).getTime() : false;`).
3. In the `fetchSubmissions` effect, remove the line `if (data.finalClosureDate !== undefined) setFinalClosureDate(data.finalClosureDate);`.
4. In the year group section, replace the entire `<TooltipProvider>...</TooltipProvider>` block with a simple Button that is only disabled while a download is in progress:
   ```tsx
   <Button
     size="sm"
     onClick={() => handleDownloadZip(yearId, label)}
     disabled={downloadingYearId !== null}
     className="gap-2 bg-slate-900 text-white hover:bg-slate-800"
   >
     {downloadingYearId === yearId ? (
       <>
         <Loader2 className="h-3.5 w-3.5 animate-spin" />
         Downloading...
       </>
     ) : (
       <>
         <Download className="h-3.5 w-3.5" />
         Download ZIP
       </>
     )}
   </Button>
   ```
5. Remove the Tooltip-related imports (`Tooltip`, `TooltipContent`, `TooltipProvider`, `TooltipTrigger`) if they are no longer used anywhere in the file. Also remove the `format` import from `date-fns` if it is no longer used elsewhere in the file (check the `formatDate` function — it uses `format`, so keep it).
6. Keep the `finalClosureDate` field in the API response from `app/api/manager/submissions/route.ts` — do NOT modify that file. It is harmless.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit --pretty false 2>&1 | head -20</automated>
    <manual>Confirm the Download ZIP button has no Tooltip wrapper and no isPastFinalClosure disabled condition</manual>
  </verify>
  <done>Download ZIP button is always enabled (only disabled during active download). No Tooltip, no closure date check in UI. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with exit code 0
2. The download route file contains no reference to `isPastFinalClosure` or `closure-guard`
3. The manager page file contains no `isPastFinalClosure` variable or `TooltipProvider` wrapping the download button
4. The button's `disabled` prop only references `downloadingYearId`
</verification>

<success_criteria>
Marketing Manager can click Download ZIP at any time regardless of the finalClosureDate. The button is never disabled except during an active download operation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-critical-fixes/06-03-SUMMARY.md`
</output>
