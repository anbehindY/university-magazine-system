---
phase: 05-ui-layer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/app-sidebar.tsx
  - components/nav-user.tsx
  - components/ui/tabs.tsx
  - app/api/academic-years/route.ts
  - app/api/guest/submissions/route.ts
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [GUEST-01]

must_haves:
  truths:
    - "Sidebar shows role-specific navigation links for each role (coordinator, manager, guest, student, admin)"
    - "Sidebar shows faculty name for scoped roles (coordinator, guest)"
    - "Sidebar has a visible logout button at the bottom"
    - "Guest user can fetch selected submissions scoped to their faculty via API"
    - "Academic years API returns id field in the response"
    - "Tabs UI primitive is available for use in reports page"
    - "SWR is installed and importable"
  artifacts:
    - path: "components/app-sidebar.tsx"
      provides: "Role-based navigation sidebar with faculty name display"
      contains: "buildPages"
    - path: "components/ui/tabs.tsx"
      provides: "Tabs component wrapper from radix-ui"
      exports: ["Tabs", "TabsList", "TabsTrigger", "TabsContent"]
    - path: "app/api/guest/submissions/route.ts"
      provides: "Guest faculty-scoped selected submissions API"
      exports: ["GET"]
    - path: "app/api/academic-years/route.ts"
      provides: "Academic years with id field in response"
      contains: "id: true"
  key_links:
    - from: "components/app-sidebar.tsx"
      to: "/api/faculties"
      via: "fetch in useEffect for faculty name lookup"
      pattern: "fetch.*api/faculties"
    - from: "app/api/guest/submissions/route.ts"
      to: "prisma.submission"
      via: "database query with isSelected and facultyId filter"
      pattern: "prisma\\.submission\\.findMany"
---

<objective>
Install SWR, create Tabs UI component, refactor sidebar for role-based navigation with faculty name display, create guest submissions API endpoint, and fix academic-years API to return id.

Purpose: Every subsequent plan depends on the sidebar showing correct role links, SWR being available for comment polling, Tabs existing for reports, the guest API existing for the guest page, and academic-years returning id for the reports year selector.
Output: Role-aware sidebar, Tabs component, guest submissions API, SWR installed, academic-years id fix.
</objective>

<execution_context>
@/home/alfie/.claude/get-shit-done/workflows/execute-plan.md
@/home/alfie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-layer/05-RESEARCH.md

@components/app-sidebar.tsx
@components/nav-user.tsx
@components/nav-main.tsx
@components/ui/switch.tsx
@app/api/academic-years/route.ts
@app/api/manager/submissions/route.ts
@app/api/coordinator/submissions/route.ts
@app/(management)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install SWR, create Tabs component, fix academic-years API to include id</name>
  <files>package.json, pnpm-lock.yaml, components/ui/tabs.tsx, app/api/academic-years/route.ts</files>
  <action>
1. Run `pnpm add swr` to install SWR for comment polling (locked decision requires SWR).

2. Create `components/ui/tabs.tsx` wrapping the `radix-ui` Tabs primitive following the exact same pattern as `components/ui/switch.tsx`:
   - `"use client"` directive at top
   - Import `{ Tabs as TabsPrimitive } from "radix-ui"` (NOT `@radix-ui/react-tabs` — project uses the monorepo package)
   - Import `{ cn } from "@/lib/utils"`
   - Export four components: `Tabs` (= `TabsPrimitive.Root`), `TabsList`, `TabsTrigger`, `TabsContent`
   - `TabsList`: `inline-flex h-9 items-center rounded-lg bg-slate-100 p-1 text-slate-500`
   - `TabsTrigger`: `inline-flex items-center justify-center whitespace-nowrap rounded-md px-3 py-1 text-sm font-medium transition-all data-[state=active]:bg-white data-[state=active]:text-slate-900 data-[state=active]:shadow-sm disabled:pointer-events-none disabled:opacity-50`
   - `TabsContent`: `mt-2 ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`
   - Use `React.forwardRef` for all three sub-components with correct displayName assignments

3. Fix `app/api/academic-years/route.ts` to include `id: true` in the select clause. The reports page needs the academic year id to pass as `?academicYearId=X` to the reports API. Currently the select only returns `yearLabel, firstClosureDate, finalClosureDate, isActive` — add `id: true`.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify swr appears in package.json dependencies</manual>
  </verify>
  <done>SWR is in package.json dependencies; components/ui/tabs.tsx exports Tabs, TabsList, TabsTrigger, TabsContent; academic-years API select includes id: true; TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor sidebar for role-based navigation with faculty name display</name>
  <files>components/app-sidebar.tsx, components/nav-user.tsx</files>
  <action>
1. Rewrite `buildPages()` in `components/app-sidebar.tsx` to return role-specific navigation items per locked decisions:
   - `MARKETING_COORDINATOR`: [{title: "Submissions", url: "/coordinator/submissions", icon: FileText}, {title: "Reports", url: "/reports", icon: ChartColumn}]
   - `MARKETING_MANAGER`: [{title: "Selected Submissions", url: "/manager/submissions", icon: CircleCheckBig}, {title: "Reports", url: "/reports", icon: ChartColumn}]
   - `GUEST`: [{title: "Selected Articles", url: "/guest/submissions", icon: CircleCheckBig}, {title: "Reports", url: "/reports", icon: ChartColumn}]
   - `STUDENT`: [{title: "My Submissions", url: "/submissions", icon: Upload}]
   - `ADMINISTRATOR`: [{title: "Closure Dates", url: "/admin", icon: Calendar}, {title: "Upload Rules", url: "/admin/upload-rules", icon: Upload}, {title: "User Management", url: "/users", icon: Users}]
   - Default (unknown role): empty array
   - Remove all `#` placeholder links and unused nav items (Dashboard, Audit Log, Notifications, Settings, etc.)

2. Remove unused icon imports from the file (LayoutDashboard, Megaphone, Settings, Shield, ClipboardList, and any others no longer referenced).

3. Add `facultyId` to the `SidebarUser` type as `facultyId?: string | null`.

4. Update `NavUser` component in `components/nav-user.tsx` to:
   - Accept `facultyId` in the user prop type
   - Fetch faculty name client-side using `useEffect` + `fetch("/api/faculties")` on mount (only when `user.facultyId` is truthy)
   - Display the faculty name below the role text (e.g., "Coordinator" on one line, "Faculty of IT" below it) — or next to role as "(Faculty of IT)" — use whatever fits the compact sidebar format
   - Store faculty name in local state; while loading, show nothing extra (no spinner)

5. Update the `AppSidebar` component to pass `facultyId` from the user prop through to NavUser. The `SidebarUser` type should include `facultyId`. The management layout passes `user` from `getCurrentUser()` which includes `facultyId` on the session user.

6. Update the sidebar header text from "Admin Portal" / "Enterprise Control Panel" to "University Magazine" / contextual subtitle based on role, or simply "University Magazine" for all roles.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that buildPages returns correct items for each role by reading the code</manual>
  </verify>
  <done>Sidebar shows role-specific nav items with no placeholder `#` links; NavUser fetches and displays faculty name for scoped roles; unused icons removed; header updated from "Admin Portal" to "University Magazine"; TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Create GET /api/guest/submissions for faculty-scoped selected submissions</name>
  <files>app/api/guest/submissions/route.ts</files>
  <action>
Create `app/api/guest/submissions/route.ts` with a GET handler mirroring the coordinator submissions pattern but for guest users:

1. Auth gate: `auth.api.getSession()` — return 401 if no session.
2. Role gate: `session.user.role !== "GUEST"` — return 403.
3. Faculty gate: Look up `dbUser.facultyId` via `prisma.user.findUnique({ where: { id: session.user.id }, select: { facultyId: true } })`. Return 403 if null.
4. Query: `prisma.submission.findMany()` with `where: { isSelected: true, facultyId: guestFacultyId }`, selecting `id, title, submittedAt, user: { select: { name: true } }, _count: { select: { files: true } }`.
5. Map results to flat shape: `{ id, title, studentName, submittedAt, fileCount }`.
6. Return `NextResponse.json({ submissions: result })`.

Follow the exact guard order and error message patterns from `app/api/coordinator/submissions/route.ts` and `app/api/manager/submissions/route.ts`.
  </action>
  <verify>
    <automated>cd /home/alfie/next-prisma && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>GET /api/guest/submissions returns 401 without auth, 403 for non-guest roles, 403 for guest without faculty, and returns faculty-scoped selected submissions for valid guest users; TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `pnpm add swr` succeeded and `swr` is in package.json
- `npx tsc --noEmit` passes with zero errors
- `components/ui/tabs.tsx` exists and exports four named components
- `app/api/guest/submissions/route.ts` exists and exports GET
- `app/api/academic-years/route.ts` select includes `id: true`
- `components/app-sidebar.tsx` buildPages() returns role-specific items with no `#` URLs
- `components/nav-user.tsx` fetches faculty name for scoped roles
</verification>

<success_criteria>
SWR is installed; Tabs component is available; sidebar shows correct role-specific navigation; guest submissions API endpoint exists; academic-years API returns id; all TypeScript compiles cleanly. Every subsequent Wave 2 plan can build pages without additional foundation work.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-layer/05-01-SUMMARY.md`
</output>
